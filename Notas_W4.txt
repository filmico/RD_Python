Week 4:
-------

En el primer video Ryan explica lo mismo que se resumio en el pdf/word siguiente:
	
	C:\RDojo_Python101\W3\Maya Modules.pdf

Connect Sublime to Maya
-----------------------
https://github.com/justinfx/MayaSublime

https://fredrikaverpil.github.io/2013/07/15/send-mel-python-code-from-sublime-text-to-maya/

https://polycount.com/discussion/173627/solved-in-sublime3-cant-get-maya-python-autocompletion-to-work-help

Funciones
---------

	def myFunction(myVar):
		result = "My Function takes: " + str(myVar)
		return result
		
	myFuncReturn = myFunction("Apples")	

	print myFuncReturn


Assets
------
	Crear Assets: 
		1) Seleccionar lo que queremos incluir en el asset
		2) Create -> Assets -> Asset with Transform

	Modificar Assets:
		Modify -> Assets



Variables Globales
--------------------

	http://python-textbok.readthedocs.io/en/1.0/Variables_and_Scope.html

	Para modificar el valor de una varible que se encuentra fuera de una funcion, la volvemos a declarar pero global adentro de la funcion y despues le asignamos el valor.
	Cuando 

	a = 0

	def xxx():
		global a
		a = 3
	

	Ejemplo 1:
	----------

	a = 0

	def my_function():
	    a = 3
	    print(a)

	my_function()

	print(a)	

	When we call the function, the print statement inside outputs 3 – but why does the print statement at the end of the program output 0?

	By default, the assignment statement creates variables in the local scope. So the assignment inside the function does not modify the global variable a – it creates a new local variable called a, and assigns the value 3 to that variable. The first print statement outputs the value of the new local variable – because if a local variable has the same name as a global variable the local variable will always take precedence. The last print statement prints out the global variable, which has remained unchanged.

	What if we really want to modify a global variable from inside a function? We can use the global keyword:

	Ejemplo 2:
	----------

	a = 0

	def my_function():
	    global a
	    a = 3
	    print(a)

	my_function()

	print(a)

	
	Observar el uso de diccionarios en el script 000_Ger_Test/w4/rig_arm2.py
	------------------------------------------------------------------------